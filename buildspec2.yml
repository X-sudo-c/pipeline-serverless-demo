version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      # Nothing needed! Everything is pre-installed
       - echo "=== VERIFYING PRE-INSTALLED TOOLS ==="
       - aws --version    
       - sam --version    
       - python --version 
       - pip --version 

  build:
    commands:
      - sam build
      - sam deploy --stack-name hello-world --capabilities CAPABILITY_IAM --no-confirm-changeset

  post_build:
    commands:
      - echo "✅ Deployment complete!"